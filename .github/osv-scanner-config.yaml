version: 1
filters:
  severities:
    - HIGH
    - CRITICAL
  ecosystem: CARGO
ignore:
  - id: RUSTSEC-2024-0370
    reason: "proc-macro-error is unmaintained (not a vulnerability). Pulled transitively by merge v0.1 via merge_derive. Upgrading merge to v0.2 requires 110+ code changes due to removed Option<T> blanket impl. Tracked for future migration."
    expires: 2027-03-14
